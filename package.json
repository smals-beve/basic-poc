{
  "name": "basic-poc",
  "version": "1.0.0",
  "description": "Welcome to the `basic` repository of the **e-Gov 3.0** project.",
  "main": "index.js",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve",
    "build": "cross-env ELEVENTY_ENV=production npx @11ty/eleventy"
  },
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
